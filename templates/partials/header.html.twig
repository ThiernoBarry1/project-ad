<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{path('homePage')}}">SymfBnB</a>
     <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
     </button>
     <div class="collapse navbar-collapse" id="navbarColor02">
         <ul class="navbar-nav mr-auto">
         <li class="nav-item">
            <a class="nav-link" href="{{path('homePage')}}">Accueil</a>
         </li>
         <li class="nav-item">
             <a class="nav-link" href="{{path('ads_index')}}">Annonces</a>
         </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
               <li class="nav-item dropdown">
                   <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="account-dropdownLink">
                       <img src="{{app.user.picture}}" class="avatar avatar-mini"alt="avatar de {{app.user.fullName}}"> {{app.user.fullName}}
                   </a>
               <div class="dropdown-menu dropdown-menu-rigth" aria-labelledby="account-dropdownLink">
                    <a href="{{path('new_ad')}}" class="dropdown-item">créer une nouvelle annonce</a>
                   <a href="{{path('account_index')}}" class="dropdown-item">Mon profil</a>
                   <a href="{{path('account_update')}}" class="dropdown-item">Modifier mon profil</a>
                   <div class="dropdown-divider"></div>
                    <a href="{{path('account_logout')}}" class="dropdown-item">Déconnexion</a> 
               </div>
            </li>
            {% else %}
               <li class="nav-item">
                    <a href="{{path('account_register')}}" class="nav-link">s'incrire</a>
                </li>
                <li class="nav-item">
                    <a href="{{path('account_login')}}" class="nav-link">connexion</a>
                 </li>
            {% endif %}
         </ul>
    </div>
</nav>
{% for label , messages in app.flashes %}
    <div class="container mt-3">
        <div class="alert alert-{{label}}">
         {% for message in messages %}
             <p>{{  message | raw }}</p>
         {% endfor %}
        </div>
    </div>
 {% endfor %}